#include<stdio.h>
int main()
{
    switch(put an interger){
    case 1:
    someting;break;}
    for(incrimint;condition;update)
    {body;}
    while(condition){body;}
    if(not put any binary value)
    {body;}
    else{body;}
    do {
        body;
    }
    while(condition);
}